import { BigListPipePipe } from './big-list-pipe.pipe';

describe('BigListPipePipe', () => {
  it('create an instance', () => {
    const pipe = new BigListPipePipe();
    expect(pipe).toBeTruthy();
  });
});
